apiVersion: v1
kind: Service
metadata:
  name: abstergo-website
  namespace: default
spec:
  type: NodePort
  selector:
    app: abstergo-website
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
